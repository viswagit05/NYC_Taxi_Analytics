-- Final analytical queries for NYC taxi analytics

-- Example query placeholder
SELECT
    pickup_datetime,
    dropoff_datetime,
    passenger_count,
    trip_distance,
    fare_amount
FROM nyc_taxi_trips
LIMIT 100;
